import math
import json

from cayleypy import CayleyGraph, prepare_generators


def update_dataset(dataset_name, keys, eval_func):
    pass


def compute_lrx_growth(initial_state):
    n = len(initial_state)
    prepare_generators(n=n)
    pass